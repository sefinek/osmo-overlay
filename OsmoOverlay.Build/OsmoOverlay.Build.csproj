<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>

    <AssemblyName>OsmoOverlay.Build</AssemblyName>
    <RootNamespace>OsmoOverlay.Build</RootNamespace>

    <AssemblyTitle>OsmoOverlay Build</AssemblyTitle>
    <Description>Builds the OsmoOverlay release packages for every supported OS and architecture.</Description>
  </PropertyGroup>

</Project>
